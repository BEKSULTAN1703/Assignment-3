public class Mp4PlayerAdapter implements VideoPlayer {
    private Mp4Player mp4Player;

    public Mp4PlayerAdapter(Mp4Player player) {
        this.mp4Player = player;
    }

    @Override
    public void play(String fileName) {
        mp4Player.playMp4(fileName);
    }
}
